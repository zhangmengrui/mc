name: Build PDF

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup TeX Live
      uses: xu-cheng/latex-action@v2
      with:
        root_file: Appendix.tex # 您的主.tex文件的名称

    - name: Compile LaTeX document
      run: pdflatex main.tex

    - name: Upload PDF
      uses: actions/upload-artifact@v2
      with:
        name: Compiled-PDF
        path: Appendix.pdf

